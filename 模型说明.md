# TwinBrain 模型说明文档

## 概述

TwinBrain是一个基于多模态脑成像数据的数字孪生脑系统，用于大脑活动建模、预测和可视化。

## 模型架构

### 核心组件

TwinBrain采用异构图神经网络（Heterogeneous GNN）架构，主要包括：

1. **多模态数据处理**
   - fMRI数据：大脑区域活动（BOLD信号）
   - EEG数据：电生理活动
   - 结构连接（DTI）：大脑区域间的物理连接
   - 功能连接：大脑区域间的功能关联

2. **图神经网络层**
   - 节点表示：每个脑区作为一个节点
   - 边表示：脑区之间的连接（结构性和功能性）
   - 消息传递：通过图卷积学习脑区间的相互作用
   - 时序建模：使用LSTM/GRU捕获时间动态

3. **预测模块**
   - 单步预测：预测下一个时间点的大脑状态
   - 多步预测：预测未来多个时间点的演化轨迹
   - 刺激响应：模拟外部刺激对大脑的影响

## 数据格式

### 输入数据

TwinBrain支持多种数据格式：

1. **脑区数据**（必需）
   - 每个脑区的活动值（fMRI BOLD信号、EEG等）
   - 脑区坐标（MNI空间或FreeSurfer表面）
   - 时间序列数据

2. **连接矩阵**（可选）
   - 结构连接矩阵（DTI纤维追踪）
   - 功能连接矩阵（相关性分析）

3. **脑图谱**
   - 支持多种标准图谱：Schaefer（100/200/400区）、AAL、Destrieux等
   - 支持FreeSurfer个性化图谱

### 输出数据

1. **JSON格式**（用于Unity可视化）
   ```json
   {
     "version": "2.0",
     "timestamp": "2024-...",
     "metadata": {
       "subject": "subject_id",
       "atlas": "Schaefer200",
       "time_point": 0
     },
     "brain_state": {
       "regions": [...],
       "connections": [...],
       "global_metrics": {...}
     }
   }
   ```

2. **Python对象**（用于进一步分析）

## 训练模型

### 基本训练

```bash
# 使用默认配置
python main.py train --config config/default.yaml

# 指定输出目录
python main.py train --config config/default.yaml --output results/my_experiment

# 从检查点继续训练
python main.py train --config config/default.yaml --resume results/checkpoint.pt
```

### 配置参数

主要配置参数（在config/default.yaml中）：

```yaml
# 模型参数
model:
  hidden_dim: 128        # 隐藏层维度
  num_layers: 3          # GNN层数
  dropout: 0.1          # Dropout率
  
# 训练参数
training:
  batch_size: 32        # 批次大小
  learning_rate: 0.001  # 学习率
  num_epochs: 100       # 训练轮数
  
# 数据参数
data:
  atlas: "Schaefer200"  # 脑图谱
  sequence_length: 50   # 序列长度
```

### 监控训练

训练过程中会输出：
- 损失曲线
- 验证指标
- 最佳模型保存

## 使用训练好的模型

### 加载模型

```python
from train.trainer import load_model

# 加载训练好的模型
model = load_model("results/hetero_gnn_trained.pt")
```

### 预测

```python
# 单步预测
next_state = model.predict(current_state)

# 多步预测
future_states = model.predict_multi_step(current_state, n_steps=10)

# 刺激模拟
stimulated_state = model.simulate_stimulation(
    current_state,
    target_regions=[10, 20, 30],
    amplitude=0.5
)
```

### 与Unity集成

```bash
# 导出数据到Unity
python setup_unity_project.py

# 启动实时服务器
python -m unity_integration.realtime_server
```

详见《Unity使用指南.md》。

## API参考

### 主要类

1. **TwinBrainModel**
   - `__init__(config)`: 初始化模型
   - `forward(data)`: 前向传播
   - `predict(state)`: 单步预测
   - `predict_multi_step(state, n_steps)`: 多步预测

2. **Trainer**
   - `train(model, data_loader)`: 训练模型
   - `evaluate(model, data_loader)`: 评估模型
   - `save_checkpoint(path)`: 保存检查点

3. **DataLoader**
   - `load_brain_data(path)`: 加载脑数据
   - `preprocess(data)`: 数据预处理
   - `create_batches(batch_size)`: 创建批次

### 工具函数

```python
# 数据预处理
from preprocess import prepare_data
data = prepare_data(raw_data, atlas="Schaefer200")

# 可视化
from utils.visualization import plot_brain_state
plot_brain_state(brain_state, output_path="brain.png")

# 导出Unity格式
from unity_integration import export_to_unity
export_to_unity(brain_states, output_dir="unity_output")
```

## 性能优化

### 训练优化

1. **使用GPU**
   ```bash
   python main.py train --config config/default.yaml --device cuda
   ```

2. **调整批次大小**
   - GPU内存充足：增加batch_size（32→64→128）
   - GPU内存不足：减少batch_size或使用梯度累积

3. **学习率调度**
   - 使用余弦退火或步进衰减
   - 配置文件中设置scheduler参数

### 推理优化

1. **批量预测**
   ```python
   # 批量处理多个状态
   predictions = model.predict_batch(states)
   ```

2. **模型量化**（未来支持）
   - INT8量化
   - 动态量化

## 常见问题

### Q: 训练很慢怎么办？
A: 
1. 确保使用GPU：`--device cuda`
2. 增加批次大小
3. 减少序列长度
4. 使用较小的图谱（如Schaefer100）

### Q: 模型不收敛？
A: 
1. 检查学习率（尝试0.0001-0.01）
2. 检查数据归一化
3. 增加训练轮数
4. 调整模型复杂度

### Q: 预测结果不准确？
A: 
1. 增加训练数据
2. 调整模型架构（增加层数/维度）
3. 使用数据增强
4. 检查数据质量

### Q: 如何使用自己的数据？
A: 
1. 准备数据格式（参考示例数据）
2. 创建自定义数据加载器
3. 调整配置文件中的数据路径
4. 训练模型

## 引用

如果使用TwinBrain，请引用：

```bibtex
@software{twinbrain2024,
  title={TwinBrain: Digital Twin Brain System},
  author={TwinBrain Team},
  year={2024},
  url={https://github.com/sheinclotho/twinbrain}
}
```

## 更多资源

- 详细使用示例：参见 Unity使用指南.md
- Unity集成：参见《Unity使用指南.md》
- 最新更新：参见《更新日志》
