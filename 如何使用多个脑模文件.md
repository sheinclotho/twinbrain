# 如何在 Unity 生成过程中使用多个脑模文件

## 问题说明

您有以下脑模文件：
- `lh.pial` - 左半球表面
- `rh.pial` - 右半球表面  
- `lh.Schaefer2018_200Parcels_7Networks_order.annot` - 左半球注释
- `rh.Schaefer2018_200Parcels_7Networks_order.annot` - 右半球注释

这些是 **FreeSurfer** 格式的文件，现在 TwinBrain 已经支持直接使用这些文件！

## 解决方案

### 方法一：使用 Python API（推荐）

```python
from unity_integration import run_unity_workflow, WorkflowConfig

# 配置您的文件路径
config = WorkflowConfig(
    data_source='freesurfer',  # 使用 FreeSurfer 数据
    
    # 指定您的4个文件路径
    freesurfer_lh_surface='路径/lh.pial',
    freesurfer_rh_surface='路径/rh.pial',
    freesurfer_lh_annot='路径/lh.Schaefer2018_200Parcels_7Networks_order.annot',
    freesurfer_rh_annot='路径/rh.Schaefer2018_200Parcels_7Networks_order.annot',
    
    # 输出目录
    output_dir='output/my_brain',
    
    # 导出格式
    export_formats=['json', 'obj'],
    
    # 是否导出完整的大脑表面（可选）
    export_surface_mesh=True,  # True = 导出真实大脑表面
)

# 一键生成所有文件
results = run_unity_workflow(config)
print(f"完成！生成了 {len(results['output_files'])} 个文件")
```

### 方法二：使用命令行

```bash
python unity_automation.py \
    --freesurfer \
    --lh-surface 路径/lh.pial \
    --rh-surface 路径/rh.pial \
    --lh-annot 路径/lh.Schaefer2018_200Parcels_7Networks_order.annot \
    --rh-annot 路径/rh.Schaefer2018_200Parcels_7Networks_order.annot \
    --export-surface \
    --output my_brain_output
```

## 数据处理流程

系统会自动：

1. **加载表面文件**（lh.pial, rh.pial）
   - 读取大脑表面的三维网格数据
   - 包含左右半球的所有顶点和面

2. **加载注释文件**（.annot）
   - 读取 200 个脑区的分割信息
   - 提取每个脑区的标签和网络归属

3. **计算脑区位置**
   - 自动计算每个脑区的质心位置
   - 基于表面顶点的平均位置

4. **生成 Unity 文件**
   - **JSON 文件**: 每个时间点的脑区活动数据
   - **OBJ 文件**: 
     - `brain_regions.obj` - 200个球体代表脑区
     - `brain_surface_bilateral.obj` - 真实的大脑表面（如果启用）
   - **配置文件**: Unity 项目配置

## 输出文件结构

```
output/my_brain/
├── json/                              # 时间序列数据
│   ├── brain_state_0000.json         # 第0个时间点
│   ├── brain_state_0005.json         # 第5个时间点
│   └── ...
├── obj/                               # 3D 模型
│   ├── brain_regions.obj              # 200个脑区（球体）
│   └── brain_surface_bilateral.obj   # 完整大脑表面
├── materials/                         # 材质配置
│   ├── RegionMaterial.json
│   └── ConnectionMaterial.json
└── unity_config.json                  # Unity 配置
```

## 关键点说明

### 1. 为什么有4个文件？

FreeSurfer 将大脑分为左右半球，每个半球有：
- **表面文件** (.pial): 大脑皮层的3D形状
- **注释文件** (.annot): 脑区的分割和标签

所以总共是 2 × 2 = 4 个文件。

### 2. 系统如何处理多个文件？

系统会：
1. 分别加载左右半球
2. 自动合并为一个完整的脑图谱
3. 200个脑区（左右各约100个）都会被正确识别
4. 生成统一的坐标系统

### 3. 与 convert.py 的区别

- **convert.py**: 您之前手动转换用的脚本
- **新方法**: 系统自动处理，无需手动转换
- **优势**: 
  - 自动提取脑区信息
  - 生成完整的时间序列数据
  - 直接生成 Unity 配置

## 完整示例

我已经创建了一个示例脚本 `example_freesurfer_unity.py`：

```bash
# 运行示例
python example_freesurfer_unity.py
```

这个脚本会：
1. 检查您的 FreeSurfer 文件
2. 自动生成 Unity 所需的所有文件
3. 提供详细的使用说明

## 下一步

1. **准备文件**: 将您的4个文件放在一个目录中
2. **运行脚本**: 使用上面的 Python 代码或命令行
3. **导入 Unity**: 
   - 将生成的 OBJ 文件拖入 Unity
   - 使用 JSON 文件加载动画数据
   - 参考 unity_config.json 设置参数

## 详细文档

更多信息请查看：
- [FreeSurfer使用指南](docs/FreeSurfer使用指南.md) - 完整的使用教程
- [Unity工作流说明](docs/Unity工作流说明.md) - Unity集成指南

## 需要帮助？

如果遇到问题，请检查：
1. 文件路径是否正确
2. FreeSurfer 文件格式是否正确
3. 是否安装了所需依赖（nibabel）

祝您使用顺利！🎉
